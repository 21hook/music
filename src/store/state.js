import {playMode} from 'common/config'

const state = {
  singer: {},
  disc: {},
  topList: {},
  playList: [],
  sequenceList: [],
  mode: playMode.sequence,
  currentIndex: -1
}

export default state
